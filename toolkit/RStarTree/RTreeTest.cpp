#include <stdio.h>
#include <iostream>
#include <fstream>
#include <string>

#include "RStarTree.hpp"
#define DIM 10
int main()
{
	// typedef RTree<int, double, DIM> IndexTree;
	// std::cout<<"DimNum: "<<DIM<<"\n";
	// IndexTree ImageSearch;
	// double **features = new double *[10];
	// for(int i = 0; i < 20000; i++)
	// {
	// 	features[i] = new double[10];
	// 	for(int j = 0; j < 9; j++){
	// 		features[i][j] = (i + j) / 10;
	// 	}
	// }
	// for(int index = 0; index < 20000; index++)
	// 	ImageSearch.Insert(features[index], features[index], index);
	// double *ff = new double[10];
	// for(int i = 0; i < 10; i++)
	// 	ff[i] = ((i + 3)/(i+1) + i)/10;
	// int *Res = ImageSearch.KNNSearch(ff,20);
	// delete []Res;
	// return 0;
}
